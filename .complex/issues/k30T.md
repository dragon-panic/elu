## Scope

Wire \`elu fsck --repair\` to \`Store::fsck_repair\`.

Stub: \`cmd/fsck.rs:10\`. PRD: \`docs/prd/cli.md:357-364\`.

## Lower-ring dep

\`WKIW.3idQ.9BsS\` (Store::fsck_repair) — must land first.

## Slice

1. **Red** — CLI test: corrupt store in a recoverable way, run \`elu fsck --repair\`, assert exit code 0 and a follow-up \`elu fsck\` is clean.
2. **Green** — call repair, render report.