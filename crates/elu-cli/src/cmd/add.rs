//! `elu add` — append a dependency to the project's `elu.toml` and
//! refresh `elu.lock`. Slice 2 (cx WKIW.wX0h.BfF6).

use crate::cli::AddArgs;
use crate::error::CliError;
use crate::global::GlobalCtx;

pub fn run(_ctx: &GlobalCtx, _args: AddArgs) -> Result<(), CliError> {
    todo!("WKIW.wX0h.BfF6: implement `elu add`")
}
